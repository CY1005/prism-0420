import { test, expect } from "@playwright/test";

// Phase 2.3 子 sprint B — e2e #6 / SKELETON
// 范围：M07 / create issue + 真验证 D 类 #3 join 字段（node_name + assigned_to_name）
// 关键断言：IssueResponse DOM 含 join 字段实证
// 当前状态：SKELETON

test.describe.skip("create issue + join fields / SKELETON", () => {
  test("create issue with assignee + verify join fields render", async ({ page }) => {
    // TODO
    expect(true).toBe(true);
  });
});
