# 01 - 极简 PRD

> 极简 ≠ 简陋。3 句话能说清楚的就不写 3 段。

**状态**：accepted
**定稿日期**：2026-04-20
**第一轮完整性质疑**：通过（已基于 Reviewer 反馈补充 Q3.1 触发方式）

---

## Q1：这个项目要解决什么问题？

**现状**：CY 的产品分析、技术研究、竞品调研、测试经验散落在 Notion、Obsidian、本地文档、聊天记录中。

**痛点**：做产品分析 / 竞品对比 / 测试设计时找不齐、关联不上、版本变化无法追踪。

**需要**：一个围绕"功能模块"组织、AI 自动结构化、能持续沉淀产品理解的平台——核心是通过记录去推动产品的理解和开发（技术相关的记录、竞品分析、产品需求的评价、测试点生成）。

---

## Q2：用户是谁？

| 角色 | 核心诉求 |
|------|---------|
| **项目所有者**（CY 本人） | 集中沉淀产品理解，AI 辅助分析与测试设计，覆盖自己做的项目和学习的项目 |
| **被分享者**（同事，后期开放） | 查看竞品分析和测试点（只读，限定范围） |

> 本期只服务"项目所有者"角色，"被分享者"是后期扩展点。

---

## Q3：核心价值主张

**AI 自动结构化 + 功能模块视角的产品理解平台**

区别于 Notion / Obsidian 的自由文档：

- **AI 按 CY 工作流自动拆解整理**——手动处理的信息量太大，需要 AI 自动分析
- **围绕"功能模块"组织**——不是文档目录，而是模块为中心的整合视角
- **内置"产品评价"能力**——评价新需求：是否伪需求、有没有更好的实现方式、修改涉及哪些模块
- **内置"测试沉淀"能力**——结构化沉淀测试文档：数据结构、测试 SQL、使用方法等

---

## Q3.1：核心能力的触发方式

> 沿用 Prism 现有交互（C 完整 fork），git 导入是新增能力。

| 能力 | 触发入口 | 用户输入 | 输出形式 |
|------|---------|---------|---------|
| **AI 自动结构化** | 1. 项目页"导入数据"按钮（zip 上传）<br>2. **新增**：单独的"git 导入"按钮（clone 远程仓库 / 上传本地 .git 包） | zip / git URL / .git 包 | 4 步向导（上传 → 预览 → 映射 → 确认） |
| **功能模块视角** | 进入项目后**默认呈现**（左侧 280px 模块树 + 右侧档案页） | 点击 / 右键 / 拖拽节点 | 加载档案页 |
| **产品评价** | 档案页右上角"需求分析"按钮 → 独立工作台 `/analysis` | 粘贴需求文本 + 可选附件 + 选 AI Provider | 工作台展示：**影响范围（AI 自动分析涉及哪些已有模块，不需手动选）** / 完整性 / 合理性 / 用户视角 + 一键生成测试点 |
| **测试沉淀** | 1. 维度卡片"添加"按钮（手动）<br>2. 需求分析后"一键录入测试分析维度"（AI 提取） | 文本 + 附件 + 链接 + 代码块 / AI 生成结果勾选 | 写入维度记录 |

**与 Prism 现状的差异**：
- AI 自动结构化：新增 git 仓库 URL clone 和本地 .git 上传，**与 zip 单独入口**（Prism 设计了但未实现）
- 产品评价：强调"影响范围"是**自动分析**（AI 语义匹配找相关模块，不需手动选）
- 其他能力：完全沿用 Prism

---

## Q4：本项目与 Prism 的关系

**C. 完整 fork（计划完整重写）**

**理由**：训练完整产品设计能力，包括多人协作架构。即使本期只有 1 个用户，数据模型和架构按多人场景设计。

**接受的设计含义**：
1. 数据模型本期就要设计完整：保留 user/role/permission 字段
2. 模块清单按完整 Prism F1-F20 列：本期只设计 1 个模块，其他模块在清单里都标注
3. 设计原则按多人协作场景定：包括活动日志、并发控制、权限校验
4. 架构骨架必须包含权限层：分层架构里要有"鉴权中间件"

---

## Q5：本期目标

**把写代码前该想清楚的全部想清楚**——产出完整的：

- 极简 PRD（本文档）
- 架构骨架 6 工件（系统边界 / 技术栈 / 分层 / 模块清单 / 设计原则 / ADR）
- 工程规约决策 5 项
- 1 个最小模块的完整设计（具体模块待档位 A+B 完成后选定）

**完整 VibeCoding 训练路径**：

```
本期（前置阶段）：只做设计文档
       ↓
设计完成后：进入写代码（用 AI 实现）
       ↓
训练「设计指挥 AI 实现」的完整 VibeCoding 流程
（设计前置 → AI 实现 → 验证 → 迭代）
```

---

## 留待后续设计阶段澄清的细节

这些不阻塞 PRD 定稿，但需要在对应模块设计阶段明确：

| 待澄清项 | 归属阶段 |
|---------|---------|
| AI 自动结构化的输入/输出具体格式 | "AI 导入模块"设计阶段 |
| 评价新需求的子功能拆分（伪需求/更好实现/影响范围 是 1 个还是 3 个 API） | "产品评价模块"设计阶段 |
| 本期"最小模块"具体选哪个 | 档位 A+B 完成后选定 |

---

## 完成度判定

- [x] Q1-Q5 全部填写
- [x] Q3.1 触发方式补充（Reviewer 反馈）
- [x] AI 完整性质疑通过
- [x] CY 能用 5 分钟把以上内容讲清楚（不看文档）
